
<%= render "survey_items/forms/#{@survey_item.type.demodulize.underscore}", f: form %>


<script type="text/javascript">
	//this should rerturn either serialized form or null (e.g. when some validation errors occurs)
	function submitItemForm(){
		_form = $('form');
		textarea = $('#rich-text-area');
		if(textarea.length > 0){
			textarea.val(CKEDITOR.instances['rich-text-area'].getData());
			console.log(textarea.val())
			console.log(_form.serialize())
		}
		if(_form.validationEngine('validate'))
			return _form.serialize();
		else
			return null;

	}
</script>