<ul id="trashed-items-list">
  <% @survey.items.trashed.each_with_index do |s_item,i| %>
    <%= render "/survey_items/trashed_items/#{s_item.type.demodulize.underscore}", item: s_item, index: i %>
  <% end %>
</ul>
