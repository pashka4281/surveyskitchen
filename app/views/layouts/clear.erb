<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= yield(:head) if content_for?(:head) %>
    <title><%= content_for?(:title) ? yield(:title) : "Surveyskitchen" %></title>
    <%= csrf_meta_tags %>

    <meta name="Description" content="<%= t('layout.meta_description') %>">
    <meta name="keywords" content="<%= t('layout.meta_keywords').join(', ')%>">

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= yield :css %>
    <%= stylesheet_link_tag "marketing", :media => "all" %>
    <script type="text/javascript" src="http://vk.com/js/api/share.js?11" charset="windows-1251"></script>

    <%= favicon_link_tag '/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body class="marketing-body">

    <%= render 'shared/top_bar_marketing' %>
    <%= flash_messages %>

    <%= yield %>

    <%#= render '/shared/footer' %>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%#= javascript_include_tag "application" %>
    <%= yield :js if content_for? :js %>

  </body>
  <%= render('shared/ga_code') if 'production' == Rails.env %>
  <%= render('shared/uservoice_tab') if 'production' == Rails.env %>
  
</html>
