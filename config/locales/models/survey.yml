en:
  activerecord:
    attributes:
  models:
    survey:
      on_created_event: "Survey \"%{survey_name}\" was created"

ru:
  activerecord:
    attributes:
  models:
    survey:
      on_created_event: "Создан новый опрос: \"%{survey_name}\""